server:
  port: 0

spring:
  application:
    name: resena-servicio-test

  datasource:
    url: jdbc:h2:mem:resena_test_db;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    open-in-view: false

  flyway:
    enabled: false

services:
  catalogo:
    enabled: false
    url: http://localhost:9999

app:
  security:
    enabled: false
  webclient:
    timeout-seconds: 2

jwt:
  secret: FoodFastClaveJWTUltraSegura2026ParaMicroserviciosConMasDe42Caracteres
