spring:
  datasource:
    url: jdbc:h2:mem:inventario_servicio_test;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: false

app:
  security:
    enabled: false

jwt:
  secret: FoodFastClaveJWTUltraSegura2026ParaMicroserviciosConMasDe42Caracteres
