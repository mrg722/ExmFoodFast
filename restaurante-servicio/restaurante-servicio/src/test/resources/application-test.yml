spring:
  application:
    name: restaurante-servicio-test
  datasource:
    url: jdbc:h2:mem:restaurante_test;DB_CLOSE_DELAY=-1;MODE=MySQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    open-in-view: false
  flyway:
    enabled: false

app:
  security:
    enabled: false

services:
  notificacion:
    enabled: false
    url: http://localhost:8089

jwt:
  secret: FoodFastClaveJWTUltraSegura2026ParaMicroserviciosConMasDe42Caracteres
