spring:
  application:
    name: notificacion-servicio-test
  datasource:
    url: jdbc:h2:mem:notificacion_test_db;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    open-in-view: false
  flyway:
    enabled: false

app:
  security:
    enabled: false
  rest-client:
    timeout-seconds: 2

services:
  pedido:
    enabled: false
    url: http://localhost:8083

jwt:
  secret: FoodFastClaveJWTUltraSegura2026ParaMicroserviciosConMasDe42Caracteres
