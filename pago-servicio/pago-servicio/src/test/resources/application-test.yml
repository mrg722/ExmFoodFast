server:
  port: 0

spring:
  datasource:
    url: jdbc:h2:mem:pago_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    open-in-view: false

  flyway:
    enabled: false

services:
  pedido:
    enabled: false
    url: http://localhost:8083

app:
  security:
    enabled: false
  rest-client:
    timeout-seconds: 2

jwt:
  secret: FoodFastClaveJWTUltraSegura2026ParaMicroserviciosConMasDe42Caracteres
