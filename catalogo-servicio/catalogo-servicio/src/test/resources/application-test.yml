spring:
  datasource:
    url: jdbc:h2:mem:catalogo_test_db;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    open-in-view: false
  flyway:
    enabled: false

app:
  security:
    enabled: false
  webclient:
    timeout-seconds: 2

services:
  inventario:
    enabled: false
    url: http://localhost:8082

jwt:
  secret: FoodFastClaveJWTUltraSegura2026ParaMicroserviciosConMasDe42Caracteres
